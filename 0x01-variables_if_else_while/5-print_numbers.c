#include <stdio.h>

/**
* main - program that prints the numbers from 1 and 50
* Return: Always 0.
*/

int main(void)
{
	char letter;

	for (letter = 0; letter <= 9; letter++)

	{
	putchar(letter);
	}

	putchar('\n');

	return (0);
}
